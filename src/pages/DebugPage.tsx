import { Box, Typography } from '@mui/material';

const DebugPage = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>🔧 Debug Information</Typography>
      <Typography variant="subtitle1">Debug page temporarily disabled.</Typography>
    </Box>
  );
};

export default DebugPage;
